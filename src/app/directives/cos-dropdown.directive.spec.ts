import { CosDropdownDirective } from './cos-dropdown.directive';

describe('CosDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new CosDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
