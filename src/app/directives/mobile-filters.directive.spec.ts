import { MobileFiltersDirective } from './mobile-filters.directive';

describe('MobileFiltersDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileFiltersDirective();
    expect(directive).toBeTruthy();
  });
});
