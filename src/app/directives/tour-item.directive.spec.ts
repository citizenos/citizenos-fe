import { TourItemDirective } from './tour-item.directive';

describe('TourItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TourItemDirective();
    expect(directive).toBeTruthy();
  });
});
